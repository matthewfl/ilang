'Install:
	yaourt --needed -S base-devel imake boost bison flex snappy curl libtorrent-rasterbar intel-tbb protobuf armadillo db
	make submodule
	make			OR	make release
	ln -s i ~/bin			cp i ~/bin


imake: contains make depend
