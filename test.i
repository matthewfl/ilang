

/*
random = class {
        init: {
                // this would be run when the new is called
        }//,
//        callback: function,
  //      random: var
        
        
};
*/

what = object {
	other: 123
};

main = {
  //      something = new(random);
	Print(what.other);
};
